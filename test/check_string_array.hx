class Main {
    static function main() {
        var arr:Array<String> = ["hello", "world"];
        trace('String array type: ${Type.typeof(arr)}');
        trace('Array created successfully');
    }
}
